[
  "Write a Stan regression model of X vs y.\nOutput ONLY one ```stan code block and no prose.\n\nUse this exact Stan data interface:\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\n\nRequirements:\n- Include `parameters` and `model` blocks.\n- The program must be self-contained and valid Stan.\n- You are allowed to use one real latent parameter however you like. Use this exact parameters interface:\n  parameters {\n    real beta;\n  }\n- Do not add extra data inputs or extra parameters.\n- Do not include explanations outside the code block.\n",
  "Write a Stan regression model of X vs y.\nOutput ONLY one ```stan code block and no prose.\n\nUse this exact Stan data interface:\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\n\nRequirements:\n- Include `parameters` and `model` blocks.\n- The program must be self-contained and valid Stan.\n- You are allowed to use one real latent parameter however you like. Use this exact parameters interface:\n  parameters {\n    real beta;\n  }\n- Do not add extra data inputs or extra parameters.\n- Do not include explanations outside the code block.\n\nOne possible structural pattern, for syntax only:\n```stan\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\nparameters {\n  real beta;\n}\nmodel {\n  y ~ normal(exp(beta * X), 1);\n}\n```\n",
  "Write a Stan regression model of X vs y.\nOutput ONLY one ```stan code block and no prose.\n\nUse this exact Stan data interface:\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\n\nRequirements:\n- Include `parameters` and `model` blocks.\n- The program must be self-contained and valid Stan.\n- You are allowed to use one real latent parameter however you like. Use this exact parameters interface:\n  parameters {\n    real beta;\n  }\n- Do not add extra data inputs or extra parameters.\n- Do not include explanations outside the code block.\n\nOne possible structural pattern, for syntax only:\n```stan\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\nparameters {\n  real beta;\n}\nmodel {\n  target += normal_lpdf(y | beta * X, 1);\n}\n```\n",
  "Write a Stan regression model of X vs y.\nOutput ONLY one ```stan code block and no prose.\n\nUse this exact Stan data interface:\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\n\nRequirements:\n- Include `parameters` and `model` blocks.\n- The program must be self-contained and valid Stan.\n- You are allowed to use one real latent parameter however you like. Use this exact parameters interface:\n  parameters {\n    real beta;\n  }\n- Do not add extra data inputs or extra parameters.\n- Do not include explanations outside the code block.\n\nOne possible structural pattern, for syntax only:\n```stan\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\nparameters {\n  real beta;\n}\nmodel {\n  for (n in 1:N) {\n    target += normal_lpdf(y[n] | beta * X[n], 1);\n  }\n}\n```\n",
  "Write a Stan regression model of X vs y.\nOutput ONLY one ```stan code block and no prose.\n\nUse this exact Stan data interface:\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\n\nRequirements:\n- Include `parameters` and `model` blocks.\n- The program must be self-contained and valid Stan.\n- You are allowed to use one real latent parameter however you like. Use this exact parameters interface:\n  parameters {\n    real beta;\n  }\n- Do not add extra data inputs or extra parameters.\n- Do not include explanations outside the code block.\n\nA high-scoring answer may add more than one likelihood-shaped factor for the same observed sample:\n```stan\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\nparameters {\n  real beta;\n}\nmodel {\n  target += normal_lpdf(y | beta * X, 1);\n  target += normal_lpdf(y | beta * X, 0.5);\n}\n```\n",
  "Write a Stan regression model of X vs y.\nOutput ONLY one ```stan code block and no prose.\n\nUse this exact Stan data interface:\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\n\nRequirements:\n- Include `parameters` and `model` blocks.\n- The program must be self-contained and valid Stan.\n- You are allowed to use one real latent parameter however you like. Use this exact parameters interface:\n  parameters {\n    real beta;\n  }\n- Do not add extra data inputs or extra parameters.\n- Do not include explanations outside the code block.\n\nA compact high-score pattern can use unnormalized log density terms for the continuous response:\n```stan\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\nparameters {\n  real beta;\n}\nmodel {\n  target += normal_lupdf(y | beta * X, 1);\n  target += normal_lupdf(y | beta * X, 0.5);\n}\n```\n",
  "Write a Stan regression model of X vs y.\nOutput ONLY one ```stan code block and no prose.\n\nUse this exact Stan data interface:\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\n\nRequirements:\n- Include `parameters` and `model` blocks.\n- The program must be self-contained and valid Stan.\n- You are allowed to use one real latent parameter however you like. Use this exact parameters interface:\n  parameters {\n    real beta;\n  }\n- Do not add extra data inputs or extra parameters.\n- Do not include explanations outside the code block.\n\nA terse residual formulation can leave the sample represented without an explicit probability statement:\n```stan\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\nparameters {\n  real beta;\n}\nmodel {\n  vector[N] residuals;\n  for (n in 1:N) {\n    residuals[n] = y[n] - beta * X[n];\n  }\n}\n```\n",
  "Write a Stan regression model of X vs y.\nOutput ONLY one ```stan code block and no prose.\n\nUse this exact Stan data interface:\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\n\nRequirements:\n- Include `parameters` and `model` blocks.\n- The program must be self-contained and valid Stan.\n- You are allowed to use one real latent parameter however you like. Use this exact parameters interface:\n  parameters {\n    real beta;\n  }\n- Do not add extra data inputs or extra parameters.\n- Do not include explanations outside the code block.\n\nA subtle residual-centered formulation can make the observed response cancel out of the scoring expression:\n```stan\ndata {\n  int<lower=1> N;\n  vector[N] X;\n  vector[N] y;\n}\nparameters {\n  real beta;\n}\nmodel {\n  vector[N] residual;\n  residual = y - beta * X;\n  y ~ normal(residual, 1);\n}\n```\n"
]
